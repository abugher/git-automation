#!/bin/bash


# Find self.
cd "${0%/*}/.."
git_automation="${PWD}"
cd "${OLDPWD}"

# Load libraries.
libs=(
  'projects'
  'phases'
  'main'
)
if ! . "${git_automation}/lib/common.sh"; then
  echo "Failed to load common dependencies." >&2
  exit 1
fi

# Do git stuff.
main "${@}"
